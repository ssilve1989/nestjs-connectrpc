/**
 * Metadata key for storing the Connect RPC service descriptor on a controller class.
 */
export const CONNECT_SERVICE_METADATA = 'connect:service';

/**
 * Metadata key for storing Connect RPC method information.
 */
export const CONNECT_METHOD_METADATA = 'connect:method';

/**
 * Transport identifier for the ConnectRpc transport strategy.
 */
export const CONNECT_TRANSPORT = Symbol('CONNECT_TRANSPORT');
